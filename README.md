# Parking app cars service

This is a simple parking app that allows you to add cars.

## Installation

Use the package manager [maven](https://maven.apache.org/) to install parking app.

```bash
mvn clean install
```

## Run

```bash
mvn spring-boot:run
```

## Run tests

```bash
mvn test
```